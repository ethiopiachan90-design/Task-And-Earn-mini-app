import type { FastifyInstance } from 'fastify';

export async function taskRoutes(app: FastifyInstance) {
  // Task routes — to be implemented in Phase 3
  // GET  /api/tasks         — List active tasks
  // GET  /api/tasks/:id     — Task detail
  // POST /api/tasks         — Create task (task giver)
  // POST /api/tasks/:id/submit — Submit proof (worker)
  // GET  /api/tasks/my-submissions — My submissions
  // GET  /api/tasks/my-tasks — My posted tasks (task giver)
  // GET  /api/tasks/:id/submissions — View submissions (task giver)
  // PATCH /api/tasks/:id/submissions/:subId — Approve/reject
}
